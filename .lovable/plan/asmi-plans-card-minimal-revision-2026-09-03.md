# Asmi plans card — minimal revision

Dial the existing `asmi-plans-card.png` back to its earlier, quieter self. The card should communicate the two plans at a glance with almost no extra elements.

## What to change

- Remove the `asmi` wordmark entirely.
- Remove the amber underline beneath `ULTRA`.
- Keep only two quiet elements side by side:
  - Left: small `PRO` label, giant `20`, tiny `tasks`.
  - Right: small `ULTRA` label, giant `100`, tiny `tasks`.
- Highlight Ultra with a single, soft amber spotlight bloom behind the right panel — nothing else. No lines, no extra marks.
- Pro stays completely flat and restrained, no accent.
- Deep plum-black field, generous negative space, cream-white serif numbers, tiny uppercase sans labels.

## Deliverable

- Regenerate `/mnt/documents/asmi-plans-card.png` at 1344×512.
- No app code changes.
